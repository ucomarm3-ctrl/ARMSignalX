name: Build APK
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Buildozer Action
        uses: Peter-S/buildozer-action@v1
        with:
          workdir: .
          buildozer_version: master
          
